# One-off files
ksef-*.md

# Cache and temporary files
cache/
ccnotify/
debug/
downloads/
file-history/
history.jsonl
ide/
paste-cache/
plans/
session-env/
shell-snapshots/
stats-cache.json
statsig/
telemetry/
todos/
local/

# Plugins have their own repos
plugins/

# Project-specific data (sensitive)
projects/

# Track these:
# - CLAUDE.md (global instructions)
# - scripts/ (custom scripts)
# - settings.json (user settings)
# - commands/ (custom commands)
# Note: MCP config is in ~/.claude.json (not tracked here)
CLAUDE.md.backup
